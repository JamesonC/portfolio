---
title: About Claudio Vallejo
layout: main
meta_title: About
description: Hi there! I’m Claudio, product designer freelancing from Monterrey, Mexico. At the moment I’m looking for a job opportunity as a Product Designer working on design systems or business tools with global impact.
---
<%# Config %>
<% about = data.about %>
<% links = [] %>
<% data.press.each do |id, post| %>
  <% links.push(post) %>
<% end %>
<% data.events.each do |id, event| %>
  <% links.push(event) %>
<% end %>
<%# End %>
<main class="cf">
  <div class="mw45 center mt4-ns">
    <article class="mb4">
      <picture>
        <% about.image.breakpoints.each do |breakpoint| %>
          <source srcset="<%= breakpoint["1x"] %> 1x, <%= breakpoint["2x"] %> 2x" media="(min-width: <%= breakpoint.width %>rem)">
        <% end %>
        <img class="db w-100" src="<%= about.image["1x"] %>" srcset="<%= about.image["1x"] %> 1x, <%= about.image["2x"] %> 2x" alt="<%= about.image.alt %>">
      </picture>
    </article>
  </div>
  <section class="mw45 center ph1 ph0-ns">
    <header class="mb2">
      <h1 class="relative z-2 dib ma0 merriweather f1 fw4 dark-gray">About</h1>
    </header>
    <%# Config %>
    <% bsl = about.bio.content.length - 1 %>
    <%# End %>
    <% about.bio.content.each_with_index do |section, s| %>
      <p class="ma0 <% if s != bsl %>mb1<% else %>mb4<% end %> merriweather f1 f1_125-ns fw3 space-gray lh1_75 lh2-ns"><%= section %></p>
    <% end %>
  </section>
  <section class="mw45 center mb6 ph1 ph0-ns cf">
    <header class="mb2">
      <h1 class="relative z-2 dib ma0 merriweather f1 fw4 dark-gray">Press and Events</h1>
    </header>
    <%# Config %>
    <% rl = links.length - 1 %>
    <%# End %>
    <% links.each_with_index do |post, r| %>
      <div class="fl w-100 w-50-ns <% if r < rl %>mb1<% end %> <% if r % 2 == 0 %>pr0_5-ns<% else %>pl0_5-ns<% end %>">
        <a class="js-link shadow-0 translateY-0 duration-0_25 property-all db no-underline" href="<%= post.source %>" target="_blank">
          <table class="w-100" style="border-spacing: 0;">
            <tbody>
              <tr>
                <td class="tc w-20">
                  <%= image_tag post.avatar, :class => "v-mid" %>
                </td>
                <td class="w-80 pv1 pl1 bl b--pearl">
                  <p class="ma0 mb0_5 merriweather f1 dark-gray"><%= post.title %></p>
                  <div class="relative" style="height: 13px;">
                    <p class="js-title opacity-1 duration-0_25 property-opacity absolute ma0 pr1 lato f0_75 fw7 tracked ttu gray">
                      <%= post.subtitle %>
                    </p>
                    <p class="js-title opacity-0 duration-0_25 property-opacity absolute ma0 lato f0_75 fw7 tracked ttu gray"><%= post.link %> →</p>
                  </div>
                </td>
              </tr>
            </tbody>
          </table>
        </a>
      </div>
    <% end %>
  </section>
</main>
